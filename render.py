# Temporary Handler for render.py

import os
import sys
import logging
import argparse
import subprocess
import datetime
test = False
# Somehow Panda3D is unusable in the Linux so just replacing it with panda3d fork like Ursina Engine

#     if datetime.datetime.now() == datetime.datetime(month=8, day=31):
#         print(":) happy birthday miku")


if test == True:
    pass
else:
    raise OSError("Currently not working on release version")
